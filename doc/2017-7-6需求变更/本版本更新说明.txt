1、为防止合作方并发请求订购后向流量，合作方每批次可订购的流量包字段增加：实际可订购数量（同上一版可订购数量含义）、预扣后可订购数量（我方生成订购记录，状态为“待订购”时，预扣后订购数量减去一，如向沃家侧订购成功，则预扣后可订购数量不变；如向沃家侧订购失败，则预扣后可订购数量增加一）
2、每日0:00开始检测所有合作方下的所有可订购资源有效期，如某批次到期则将该批次状态置为失效状态；如果某批次实际可订购次数为0，则将该批次状态置为失效状态；
